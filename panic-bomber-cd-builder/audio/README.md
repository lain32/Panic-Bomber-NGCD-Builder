# Finalized audio

Place `track02.wav` through `track18.wav` here, copied from the **audio** folder in `Panic-Bomber-CD-Builder-1.0.zip`.

These WAV files are intentionally excluded from Git history. They are included in the full release download. The builder verifies their SHA-256 hashes before using them.

Use the corrected Test 4 ending as `track04.wav`. Copying the original Test 2 track04 will fail the hash check.
